# VoiceDiary 重建计划（2026-06-16 定）

## 一、决策：新建干净工程，除 ID 复用外一概重建

旧工程地基问题（`W_SCALE/H_SCALE` 等比缩放而非自适应、无设计 token、手管 `.xcodeproj`、表现层临时拼凑），逐屏打补丁无法达到设计稿精致度。决定推倒，**新建干净 SwiftUI 工程**。

- **复用（仅 ID）**：
  - Bundle ID：`com.chunqingliao.VoiceDiary`
  - App Store ID：`6670278331`
  - IAP product：`com.chunqingliao.VoiceDiary.fullunlock`（一次性买断 ¥15）
- **放弃**：旧用户与旧数据（几乎零用户）→ 无数据迁移。版本更新本就要过审，重写不额外增加负担。
- **重建范围**：
  - 表现层：全部页面 View + 一套设计系统（间距/字号/圆角/颜色 token + 基础组件），全新写。
  - 底层逻辑：SwiftData 模型、各 Manager（通知/隐私锁/购买/定位/导出/统计/诊断）、翻页引擎——**借鉴**旧实现，在新工程重写/搬迁，不照搬旧 View。
- **配色/视觉**：沿用「仿真日记本」规范：主色暖陶土橙 `#C8763A`、纸张 `#FBF3E3`、皮革 `#6B4226→#4A2C17`、暗金 `#D8B56A`、暗夜棕 `#2B2420`。

## 二、新增核心功能：语音日记（录音）

App 名为 VoiceDiary 却一直没有语音功能（名实不副）。本次补上，作为差异化主线：**「能翻页的皮质日记本 + 语音日记」**，市面少见。

### 2.1 产品形态
语音是与「文字 / 配图」并列的第三种记录方式，两种用法合一：
1. **语音附件**：一篇日记里可录一/多段语音，作为附件保留，详情页可回放（适合「懒得打字，说两句」）。
2. **语音转文字**：录音同时端侧实时转写，停止后可一键把文本插入正文（适合「说出来自动成文字日记」）。
录完弹预览：转写文本（可编辑）+ 语音条，让用户选择「插入正文」「仅存语音」「都要」「重录/删除」。

### 2.2 入口与界面（详见 voice-feature-mockups.html）
1. 写日记页工具栏新增 `🎤 语音` pill。
2. 录音中面板：暖橙波形动画 + 计时 + 实时转写滚动 + 停止键，皮革/纸张风格。
3. 录音完成预览：可编辑转写文本 + 语音条（时长/播放）+ 操作按钮。
4. 详情页：正文下方语音附件条（播放/波形/时长）。
5. 列表页：含语音的日记项显示 `🎤` 标记。

### 2.3 数据模型（在 `DiaryEntry` 上扩展）
- `voiceMemos: [VoiceMemo]`（externalStorage）：每条含音频文件、时长、转写文本。

### 2.4 技术（全苹果原生，零第三方）
- 录音：`AVAudioRecorder`（AAC/m4a），`averagePower` 驱动波形。
- 转写：`Speech.framework` `SFSpeechRecognizer`，**强制端侧** `requiresOnDeviceRecognition = true`，语言 zh-CN（按系统语言）。
- 播放：`AVAudioPlayer`。
- 权限：`NSMicrophoneUsageDescription` + `NSSpeechRecognitionUsageDescription`。

### 2.5 隐私红线（注意：不等于禁止同步）
- **绝不经过开发者 / 第三方服务器**：录音与转写全部在本机完成（Speech 强制 on-device），不调用任何云端转写 API，不上传到我们或任何第三方后端。
- **iCloud 同步保留**：数据存进**用户自己的私有 iCloud**（`NSPersistentCloudKitContainer` / CloudKit private database），端到端在苹果生态内、绑用户 Apple ID，开发者无法访问内容。这是用户要的安全同步，不违反红线。设置页保留 iCloud 开关。
- **音频同步（方案 A，待确认）**：音频也随私有 iCloud 同步（低码率 AAC 压缩，约几百 KB/分钟，占用户自己的 iCloud 空间），换设备可听原声。备选方案 B：音频仅留本机、只同步文字+转写。

### 2.6 变现
- 免费版可录短语音；「语音转文字 + 无限时长」并入现有 ¥15 一次性买断解锁点。
- iCloud 同步保持免费（用户自己的空间），不作付费墙。

### 2.7 状态
**待评审**。设计图通过后再建新工程开工。
